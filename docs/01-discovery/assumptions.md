# Assumptions Reference Template

> Sao chép ledger này vào adopter workspace. Assumption rỗng hoặc chưa giải quyết không phải product fact.

## Document control

- Document status: DRAFT
- Owner/role: `<OWNER>`
- Kernel release: `NEWERA_VERSION`
- Kernel source commit: `SOURCE_COMMIT`
- Project registry reference: `<REGISTRY-REF>`
- Created/updated: `<DATE>`

## Assumption ledger

| ID | Giả định | Cơ sở | Ảnh hưởng nếu sai | Cách kiểm chứng | Trạng thái |
|---|---|---|---|---|---|
| `ASM-<ASM-ID>` | `<ASSUMPTION>` | `<BASIS>` | `<IMPACT>` | `<CHECK-OR-RES-ID>` | OPEN |

Không trình bày giả định là yêu cầu hoặc sự thật. Giả định ảnh hưởng cao phải trở thành research item hoặc decision. Khi adopter xác nhận hoặc loại bỏ assumption, giữ lịch sử và cập nhật traceability.
